# Server environment variables — copy to .env and fill in your values
# .env is gitignored so your secrets stay private

# Long random string — agents must use this same key in their config.yaml
API_KEY=change-me-to-a-long-random-string

# Path to the SQLite database (default is fine for local/Docker use)
# DB_PATH=/data/monitor.db
